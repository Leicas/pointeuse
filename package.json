{
  "name": "pointeuse",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "tauri android init && node scripts/patch-android-gen.mjs",
    "android:patch-gen": "node scripts/patch-android-gen.mjs",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
